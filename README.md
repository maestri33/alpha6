# Alpha6

Projeto Alpha6 - V7M Platform

## Desenvolvimento

Acesse o code-server: `https://victor.alpha6.codesrv.m33.live`

## Deploy

- **Dev**: Push para `dev/*` → auto-deploy
- **Prod**: Merge em `main` → auto-deploy

## Estrutura

```
├── src/           # Código fonte
├── Dockerfile     # Build
└── docker-compose.yml
```
